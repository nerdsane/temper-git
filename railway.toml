[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'temper serve --port ${PORT:-3000} --storage postgres --no-observe --app temper-git'"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
